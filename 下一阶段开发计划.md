# 角色一键养成：下一阶段开发计划

最后更新：2026-08-28

## 计划范围

本计划只覆盖以下四项后续功能：

1. 多树脂任务连续调度。
2. 统一失败状态模型。
3. 自动读取当前角色档案并生成培养目标。
4. 树脂优先级与规则自定义。

当前 `0.9.0` 和官方 PR #3496 保持功能冻结；这些功能在个人源码仓库的后续版本中开发，完成本地验证后再决定公开版本号和提交范围。开发不修改 BetterGI 主程序，也不改变 `0.9.0` 已有设置的默认行为。

## BetterGI 基线与兼容策略

- 发布兼容基线统一为 BetterGI `0.64.0`，并要求启用 OCR V6；更早版本和其他 OCR 实现不在支持范围内。接口核对同时跟踪官方主分支 `0.64.1-alpha.1`，不把 alpha 版本作为普通用户的强制依赖。
- BetterGI `0.64.0` 的 `characterDevelopmentTask.GetCharacter()` 已能返回角色等级/等级上限、武器名称/等级/等级上限、三项天赋界面等级和命座等级加成标记；自动秘境参数提供 `ResinPriorityList`，脚本会显式写入自身配置的顺序。
- 秘境和 Boss 接口目前返回奖励字典，但没有统一返回“树脂不足、战斗失败、领奖失败”等结构化结束原因。因此失败模型以调用阶段和脚本上下文为主；只有 BetterGI 明确给出原因时才细分，不能把奖励为空直接断言为树脂不足。
- 优先检测对象、方法和返回字段是否实际存在，而不是只比较版本号。能力不足时给出明确提示，并回退到现有手动目标流程。
- 培养/圣遗物秘境的树脂类型和顺序由脚本通过 BetterGI 0.64 参数显式传入；有限总次数仍只交给第一种已授权树脂，避免分类型计数突破上限。

## 开发顺序与依赖

```text
统一失败状态模型
        ↓
树脂规则模型 V2
        ↓
多树脂任务连续调度
        ↓
自动档案接入统一校验、记录和邮件
```

自动档案的识别适配器可以在第二、三阶段期间独立编写，但只有接入统一失败模型和最终目标校验后才算完成。

每项功能使用独立 `codex/` 分支，前一阶段通过本地验证并合入开发基线后再开始下一阶段。需要游戏内验证的部分标记为“已实现、待实机验证”，不阻塞后续本地开发，也不写成已验证。

## 阶段 1：统一失败状态模型（代码与本地测试已完成，待实机验证）

### 目标

让配置检查、档案识别、背包扫描、队伍切换、Boss、秘境、路线、奖励复核和清理操作使用同一套结果结构。日志、邮件和历史记录从结构化结果生成，不再依赖各模块自行拼接状态文本。

### 数据结构

新增统一任务结果：

```js
{
  taskId,
  taskType,
  targetName,
  status,       // completed | unconfirmed | skipped | failed
  code,         // 稳定的机器可读原因
  stage,        // preflight | profile | inventory | party | task | reward | cleanup
  severity,     // info | warning | fatal
  retryable,
  message,
  evidence,
  startedAt,
  endedAt
}
```

整次运行改为：

```js
{
  status,
  stopCode,
  tasks: [],
  routes: [],
  trackedRewards: {},
  warnings: []
}
```

历史记录继续兼容旧的 `execution.task` 和 `execution.status`；迁移期由新结果推导旧字段，读取旧记录时补默认值，不批量重写用户已有历史文件。

### 首批状态码与处理规则

- `config_invalid`、`profile_invalid`、`capability_missing`：操作游戏前终止。
- `task_disabled`、`not_open`、`no_shortage`、`no_candidate`：跳过，不算错误。
- `no_resin`：停止后续树脂任务；游戏状态安全时仍允许执行不耗树脂路线。
- `party_switch_failed`、`navigation_failed`、`battle_failed`、`external_task_error`：按当前安全策略停止本次后续自动任务。
- `reward_unconfirmed`：不宣称成功，也不继续尝试下一个树脂任务；允许进入最终背包复核和报告阶段。
- `inventory_scan_failed`：保留执行前数量，收益显示未知，不按零处理。
- `cleanup_failed`：记录警告，不覆盖原任务结果。

异常分类优先由调用位置明确赋值。例如切队调用抛错直接归为 `party_switch_failed`；不能仅靠中文异常文本猜测阶段。BetterGI 明确包含“树脂不足”等信息时可以补充细分类，否则保留 `external_task_error` 或 `reward_unconfirmed`。

### 代码改造

- 新增 `core/execution-outcome.js` 和 `core/error-classifier.js`。
- 为档案、背包、切队、秘境、Boss、路线和安全退场增加统一执行包装器。
- `core/report.js`、`core/history.js` 和 `main.js` 改为消费统一结果。
- 邮件分别显示“已完成”“已跳过”“未确认”“失败”，并给出可执行的下一步，而不是直接显示原始堆栈。
- 完整异常和证据继续写日志/历史，邮件只展示简洁原因。

### 本地验证

- 每个状态码的日志、邮件、历史序列化测试。
- 旧历史记录兼容测试。
- 清理失败不覆盖任务成功/失败的测试。
- 奖励为空不会被误判为树脂不足。
- 配置错误发生在背包读取和游戏操作之前。

当前进度：已在 `codex/unified-failure-model` 分支完成统一结果、调用阶段错误分类、旧记录兼容、邮件/历史接入、路线最终状态和 Boss 清理告警；全量测试增至 83 项。BetterGI 的真实异常文本、树脂不足结束和邮件长度仍留待后续实机运行确认。

## 阶段 2：树脂优先级与规则模型 V2（代码与本地测试已完成）

### 目标

把“先刷什么”和“允许使用什么树脂”分开建模，为多任务调度提供明确、可校验的预算。默认配置必须复现当前行为。

### 配置结构

```js
{
  taskOrder: ['boss', 'limitedDomain', 'domain', 'artifactDomain'],
  taskLimits: {
    boss: { maxClaims: null },
    limitedDomain: { maxClaims: null },
    domain: { maxClaims: null },
    artifactDomain: { maxClaims: null }
  },
  resinTypes: {
    original: true,
    condensed: true,
    transient: false,
    fragile: false
  },
  routeTiming: 'afterResin',
  artifactFallbackEnabled: false
}
```

`null` 表示由兼容模式把剩余预算交给该任务，并不等于无限循环。进入多任务模式后，排在后面的任务要获得机会，前面的任务必须设置有限领取次数或运行预算。

### 配置界面

- 普通用户保留“一次只刷一类（推荐）”预设，不新增必须填写的字段。
- 高级模式使用下拉框选择优先级槽位、每类最大领取次数和路线执行时机；重复任务类型、遗漏必需类型和非法次数在操作游戏前拒绝运行。
- 须臾和脆弱树脂继续独立授权且默认关闭；启用时显示会消耗背包道具的提醒。
- 原粹树脂保留量只有在 BetterGI 提供可靠的当前树脂读取能力后才开放。接口确认前使用“本次最多领取次数/最多投入树脂”表达，不伪造精确保留功能。
- 设置页和日志展示解析后的实际任务顺序、任务上限、允许的树脂类型和跳过原因。

### 规则编译

- 新增 `core/resin-policy-v2.js`，将设置页值编译为不可变的执行策略。
- `core/preflight.js` 在运行前校验完整队列所需的队伍、策略、开放日和树脂上限，避免完成第一个任务后才发现第二个任务配置错误。
- Boss 固定只用原粹树脂；培养和圣遗物秘境继续服从 BetterGI 固定树脂顺序。
- 安全约束不能被自定义规则覆盖：执行确认、真实缺口、周日奖励序号、Boss 专属禁用、队伍必填和须臾/脆弱树脂授权始终有效。

### 本地验证

- 旧 `resinStrategy` 自动迁移为 V2 默认策略。
- 自定义排序、重复项、遗漏项、负数、零次数和超大次数校验。
- Boss 不会获得浓缩、须臾或脆弱树脂。
- 未授权消耗品不会进入 BetterGI 参数。
- 配置预览与实际编译结果完全一致。

当前进度：已新增兼容/自定义两种模式、四类任务排序槽、每类领奖上限、路线前后时机和独立树脂授权；策略会在任何背包或游戏操作前编译为不可变 V2 对象，并写入计划与历史。新增 `pnpm validate:resin` 独立预览命令及 8 项专项测试，全量测试增至 91 项。领取上限真正传入多个任务、整队列预检和路线前置执行在阶段 3 完成；实机设置页与连续调用留待集中回归。

## 阶段 3：多树脂任务连续调度（代码与本地测试已完成）

### 目标

一次运行能够按策略依次执行多个合格树脂任务，而不是只取 `todayQueue` 的第一个任务。第一版只实现“顺序＋每类上限”，不做多个秘境之间的精确比例分配。

### 执行流程

1. 开始前计算一次缺口并读取一次背包。
2. 根据 V2 策略生成有界 `executionQueue`，合并同一 Boss/秘境的多阶材料。
3. 在任何树脂任务开始前，对整条队列完成配置检查。
4. 每个队列项最多调用一次 BetterGI 任务接口；具体领取次数由该项预算控制，脚本不自行无限重试。
5. 每项完成后保存 BetterGI 奖励识别作为过程证据，但不打开背包，也不把过程奖励当成最终库存重新规划。
6. 遇到统一失败模型规定的终止状态后停止树脂队列；是否继续路线由状态规则决定。
7. 所有自动任务结束后只读取一次背包，统一计算实际收益、剩余缺口和下一次计划。

### 队列规则

- 默认顺序仍为世界 Boss → 当天限时培养秘境 → 普通培养秘境 → 圣遗物填充。
- 圣遗物仅在培养任务完成、跳过或不存在，且没有确认“树脂不足/奖励未知”时作为最后兜底；没有可靠剩余树脂证据时不反复尝试。
- 同一个具体 Boss 或秘境在一次运行中只出现一次。
- 设置硬上限限制任务总数，防止配置错误形成循环。
- 若多个任务涉及同一材料，最终背包差值只作为整次运行的材料总收益；单任务归属只使用 BetterGI 奖励证据，不凭空拆分。
- 第一版不根据中途奖励动态改变其他任务预算，不追求刚好补齐材料，允许少量多刷。

### 记录与邮件

- 历史记录保存每个任务的类型、目标、预算、结果、任务奖励证据和耗时。
- 邮件按执行顺序列出每个任务，明确完成、跳过、未确认或失败。
- 最终材料收益仍按整次运行的背包差值优先，任务奖励仅在背包漏识别时兜底，不重复累计。

### 本地验证

- Boss → 培养秘境 → 圣遗物的顺序与上限。
- 限时秘境优先于普通秘境。
- 第一个任务完成、跳过、树脂不足、奖励未确认、切队失败和异常时的后续决策。
- 所有执行器共用同一个首次神像传送/切队状态，不重复初始化。
- 整次运行始终只有前后两次背包读取。
- 队列去重、最大任务数和无循环测试。

### 后续实机验证

- 使用低额度配置依次调用两个不同树脂任务，检查不会被第一个任务耗尽全部预算。
- Boss 后继续培养秘境；培养秘境后尝试圣遗物兜底。
- 树脂不足时停止树脂队列，并验证一条龙后续和不耗树脂路线行为。
- 邮件、历史和实际背包差值一致。

当前进度：已实现兼容单任务与自定义有界队列、整队列预检、每类只选一个具体目标、最多四项硬上限、路线前/后时机、任务奖励聚合和逐项邮件状态。有限秘境预算只交给第一种已授权树脂，防止 BetterGI 的分类型计数突破总上限；任务失败、奖励未确认或明确树脂不足时停止后续树脂任务。新增 9 项队列专项测试，全量测试增至 100 项；连续 BetterGI 调用、有限预算树脂退出和路线前置仍待集中实机验证。

## 阶段 4：自动读取当前档案并生成目标（代码与本地测试已完成，待实机验证）

### 用户配置

- 选择角色。
- 选择角色目标等级；界面明确标注目标为突破后的等级状态。
- 分别选择普通攻击、元素战技、元素爆发目标等级。
- “培养内容”使用下拉框：角色和当前佩戴武器、角色和指定武器、仅角色、仅指定武器。
- 自动读取当前佩戴武器时选择目标等级；指定武器时选择名称及当前→目标区间。
- 仅指定武器不要求选择角色，也不调用角色档案接口。

### 档案适配器

- 新增 `core/character-profile.js`，优先调用：

```js
await characterDevelopmentTask.GetCharacter(characterName, '属性;武器;天赋');
```

- 将 ClearScript/.NET 返回值转换为普通对象，并保存原始结果用于排查。
- `Level + LevelLimit`、`WeaponLevel + WeaponLevelLimit` 用于区分 `80/80` 与 `80/90`，内部采用 `{ level, levelLimit }`，避免“突破前/后”歧义。
- `SkillHasBonus`、`BurstHasBonus` 等为真时，将界面显示的命座 `+3` 转换为实际培养等级；转换后必须仍在合法范围内。
- 自动武器模式下，武器名需通过规则库校验；一星初始武器按 `rarity=1` 自动忽略。
- 角色等级、三项天赋和武器分别生成状态；当前进度达到或超过目标时只跳过该项。字段为空、名称未知或等级非法时仅停止受影响的目标，不回退为 1 级或猜测武器；没有任何有效目标时才终止整次运行。

### 模式与确认

- 设置页提供“手动目标”“自动档案仅预览”“自动档案识别后执行”三种模式。
- “识别后执行”仍要求顶层执行确认；运行日志在背包扫描和调度前完整展示原始档案、规范化当前状态和最终目标。
- “仅预览”只生成 `record/latest-profile.json`，不读取背包、不执行刷取任务，便于首次排查识别结果；它是可选工具，不强制所有用户先测试。
- 识别错误进入统一失败状态和邮件提示；文字识别异常建议检查 OCR V6，但脚本不宣称能检测或切换 OCR 版本。

### 目标与报告

- 自动档案转换为现有计算器使用的标准角色/武器目标，不为自动模式维护第二套材料算法。
- `latest-plan.json`、历史和邮件增加：当前角色等级/上限、三项实际培养等级、当前武器及等级/上限，以及用户选择的目标。
- 手动模式也从现有目标区间生成同样的“当前 → 目标”摘要，使邮件格式一致。

### 本地验证

- 普通角色、旅行者、角色别名和未知角色。
- `80/80`、`80/90`、已满级和目标低于当前状态。
- 零命天赋、命座 `+3`、普攻无加成和异常加成标记。
- 五种武器类型、普通武器、一星初始武器、未知武器、不培养武器和手动指定武器。
- BetterGI 接口不存在、旧返回结构、字段为空和 ClearScript 对象转换。
- 自动目标与等价手动目标的材料需求逐项一致。

### 后续实机验证

- 至少选择两名不同武器类型角色，核对角色等级上限、三项天赋和佩戴武器。
- 覆盖一名有天赋命座加成的角色。
- 覆盖四种培养内容，以及角色完成后武器继续、武器完成后角色继续、全部完成直接结束。
- 识别结果、最终目标、背包缺口、邮件摘要和手动计算结果一致。

当前进度：已完成档案适配器、四种培养内容、角色/天赋/武器独立状态、`80/80` 与 `80/90` 突破边界、一星武器忽略、能力检测、预览记录、计划/历史/邮件摘要接入。角色达到或超过目标时不会再连带终止指定武器；仅指定武器不会读取角色档案。完整武器/天赋字段及识别后连续调度仍待集中实机验证。

## 集成验收标准

- 未配置任何新字段时，行为与当前版本一致：一次只执行首个树脂任务，现有目标输入和树脂策略继续有效。
- 开启多任务后，执行顺序和每项预算与配置预览一致，没有无限循环或隐式使用须臾/脆弱树脂。
- 整次运行仍只读取开始前和全部任务结束后两次背包。
- 所有任务、路线和识别失败都能映射到统一状态，邮件不会把未知结果写成成功或零收益。
- 自动档案不会使用缺失、未知或未经校验的数据生成目标；一星初始武器能正确忽略。
- 自动档案邮件能清晰显示当前角色、天赋、武器状态和最终目标。
- 运行 `pnpm test`、全部 JavaScript `node --check`、JSON 校验和 `git diff --check`；生产子集不包含测试、开发数据或本机路径。

## 分支与测试安排

1. `codex/unified-failure-model`：完成阶段 1，本地测试后保留待实机项。
2. `codex/resin-policy-v2`：基于已合入的阶段 1，完成阶段 2。
3. `codex/multi-resin-scheduler`：基于阶段 1、2，完成阶段 3。
4. `codex/automatic-profile`：完成阶段 4，并接入统一结果与邮件。
5. `codex/next-integration`：只用于四项功能合并后的整体回归，不直接开发独立功能。

每个功能完成后更新 `开发TODO.md`、`项目计划.md` 和 `技术方案.md`。需要实机测试时提供一份独立、低消耗的测试配置和明确预期；实测前不把对应项标记为完成验证。

## 集成审计结果（2026-08-20）

- 四个阶段均已在独立分支完成，并汇总到 `codex/next-integration`；未改动 BetterGI 主程序，未同步官方 PR。
- 未填写新设置时继续使用手动目标、兼容树脂规则和单树脂任务行为；自动档案与连续队列都必须由用户显式选择。
- 全量单元测试 `114/114` 通过；43 个 JavaScript 文件语法检查、17 个 JSON 文件解析、树脂规则独立预览和 `git diff --check` 均通过。
- 规则库 235 把武器均带合法稀有度，其中 5 把一星初始武器可被自动档案安全忽略；提交内容未发现本机绝对路径。
- 仍需实机验证的项目集中保留在 TODO：完整角色/天赋/武器档案、命座加成、一星武器、自动档案识别后调度、多树脂连续调用、有限预算退出和真实异常分类。未实测项目不作为已验证能力发布。
