# 角色一键养成项目计划

最后更新：2026-08-20

多树脂连续调度、统一失败状态、自动档案和树脂规则自定义的详细实施顺序见 [`下一阶段开发计划.md`](下一阶段开发计划.md)。

## 成功标准

在不自动升级角色/武器、不刷摩拉/经验书/元素玉的前提下，用户填写培养目标后，脚本能安全计算缺口，按每周开放日安排已验证培养材料秘境，消耗已有树脂并通过背包复核判断进度。未知来源必须提示而非盲目执行。

## 阶段 0：基础能力（已完成）

- 角色/武器目标、等级/天赋成本、3:1 合成链和背包缺口；合成只计算确定的基础配方，不预估角色天赋的概率返还或额外产出。培养内容可选择角色和当前佩戴武器、角色和指定武器、仅角色或仅指定武器。
- BetterGI 设置页支持按元素选择角色、按类型选择手动武器和选择目标等级；地方特产与怪物路线拥有独立开关。配置按使用频率排列且区块间有分隔线，旧配置值仅在内部迁移，不继续暴露已取消的手动角色输入。
- 首批秘境自动执行：太山府「繁荣」、震雷连山密宫「孤云」。
- 周日奖励序号、切队、神像恢复和树脂优先级。

验收：用户选择培养目标并显式确认执行后，可自动刷取已验证秘境，且不合成树脂、不领取错误的周日奖励。单次模式仅作为自愿验证工具，不是首次使用的必经步骤。

## 阶段 1：全培养材料秘境（当前优先）

1. [-] 已建立全部天赋书、武器突破材料的来源 → BetterGI 秘境名称映射表（42 条来源、14 个材料秘境）；待集中实机回归。
2. 对每条映射记录版本、周日组别和至少一次实机验证结果。
3. [x] 正式路径默认开启秘境、Boss 和圣遗物的任务级奖励识别；整次运行仍只在开始前、全部任务结束后各读取一次背包。背包差值可信时优先，结束背包漏识别时用任务奖励兜底；两者均无证据时只报告“未确认”。
4. [-] 已实现按开始时缺口安排最多四类树脂任务、全部执行后统一复核的有界队列；兼容模式保持单任务，自定义模式按上限连续执行，待低额度实机回归。
5. 基于世界等级 9 固定掉落机制估算世界 Boss 与培养材料秘境的剩余树脂和预计完成天数；路线只使用背包差值样本。

验收：任意只需要秘境培养材料的角色/武器目标，都能在一周开放日循环内自动选择正确秘境，并持续运行到材料满足或资源耗尽。

当前回归进度：周日第 1/2/3 项的计划参数已验证；2026-08-02 已在昏识塔实际选择第 3 项并完成 3 次浓缩树脂领奖，第 2 项仍未找到实际领奖记录。只消耗一次原粹树脂的培养秘境单次模式也已在无光的深都和昏识塔完成实测。

## 阶段 2：路线材料（后续候选）

1. 调用已订阅的地方特产/怪物路线，并支持文件路径人工覆盖。
2. 分别切换采集队、怪物材料队；确认路线缺失、卡死和战斗失败的停止策略。
3. 用整次运行前后的背包总差值计数采集收益；不在每条 JSON 后读取背包，本次选中的路径全部执行。路线 CD/效率先不纳入核心决策。

验收：有已订阅路线时可执行并统计收益；没有路线时发送清晰的订阅/配置提醒。

当前回归进度：月莲 10 条路线确认获得 49 个；蕈兽共享路线只执行一次并同时统计三阶材料。错误采集队只尝试两次，随后可继续使用另一队伍执行怪物路线，不再按每个 JSON 循环切队。

## 阶段 3：世界 Boss 与周本边界

已完成 BetterGI 内置首领的静态映射与独立队伍配置。世界 Boss 根据材料缺口自动匹配；少量特殊 Boss 可通过紧凑文本覆盖队伍、策略或单独禁用，名称会在执行前校验。世界 Boss 使用 BetterGI 自动首领的“刷至原粹树脂不足”模式且禁用须臾、脆弱树脂。急冻树、隐山猊兽和霜夜巡天灵主均已完成自动战斗、领奖和材料增长验证；守望者·堕天、蕴光月幻蝶有任务调用记录，但未确认领奖收益，不能算完整通过。周本自动执行不进入 0.9.0：保留材料映射用于计算缺口和手动提示，不进入执行队列，也不阻塞其他任务。

自动首领调用结束或异常后统一传送一次七天神像，防止 BetterGI 在树脂不足退出时把角色留在已刷新的 Boss 面前；不启用“每轮返回神像”。任务奖励字典兼容 ClearScript 的 `.NET Dictionary` 索引器与枚举器，供结束背包漏识别时兜底。两项修复已完成自动测试，等待下一次正常 Boss 运行复核。

预计完成时间中，世界 Boss 按世界等级 9 的 `3.1` 个/40 树脂计算，并按每日树脂预算显示约需天数；周本作为手动材料不显示预计天数，也不遮挡同一计划中 Boss/秘境的估算。

## 阶段 4：圣遗物秘境填充（纳入 0.9.0，已完成实机验证）

- 已实现为当天没有已启用世界 Boss 或培养材料秘境时的可选兜底；用户从 BetterGI 内置圣遗物秘境中选择目标，并配置独立队伍与策略。
- 沿用普通秘境的树脂规则，默认不自动分解圣遗物。2026-08-06、08-07、08-09 已使用“月童的库藏”完成实机运行；日志确认了队伍切换、进本、战斗、连续领奖和任务结束。其他圣遗物秘境不再视为执行链路未验证，但仍未逐个完成回归。
- 圣遗物不显示预计完成天数。
- 圣遗物执行结果不通过目标培养材料差值判断；摘要只说明任务已调用以及圣遗物收益未纳入材料计数，避免错误显示“没有刷取”。

## 阶段 5：树脂优先级与规则自定义（代码已完成，待实机验证）

- 配置页明确区分“任务类型优先级”和“任务内部允许使用的树脂类型”。兼容模式固定为世界 Boss → 当天限时培养秘境 → 普通培养秘境 → 圣遗物填充，并且一次只选首个合格任务；自定义模式才启用连续队列。周本材料只提示手动获取。
- 当前世界 Boss 只用原粹树脂；培养材料与圣遗物秘境按浓缩 → 原粹 → 须臾 → 脆弱的 BetterGI 固定顺序，从用户选择的树脂类型中依次消耗；不自动合成浓缩树脂。
- 已新增兼容模式与自定义模式。自定义模式通过四个防重复级联槽同时选择任务类别与本次领奖次数，并选择路线在树脂任务前或后；须臾/脆弱树脂继续单独授权且默认关闭。规则会在游戏操作前编译、校验并写入计划/历史，`pnpm validate:resin` 可独立预览。
- 自定义模式已接入最多四项的连续队列，每个类别只选一个具体目标；完整队列会在任何任务开始前校验。完成后继续下一项，失败、奖励未确认或明确树脂不足时停止。有限秘境预算只使用第一种已授权树脂，保证 BetterGI 分类型计数不会突破总上限。
- 暂不提供原粹树脂精确保留量：BetterGI 当前没有可靠的 JS 剩余树脂查询接口。自定义规则不能覆盖开放日、材料缺口、执行确认、队伍、已验证映射、周日奖励序号以及 Boss 只用原粹树脂等安全约束。
- 自定义规则只决定“先执行哪个合格任务、执行到什么上限”，不做多个秘境之间的精确比例分配。
- 配置保存前校验重复项、遗漏项、冲突条件和非法数值；后续独立配置预览必须展示解析后的实际执行顺序、各任务上限与跳过原因。

## 阶段 6：自动读取当前档案并生成目标（代码已完成，待实机验证）

- 设置页已取消角色手动填写，只保留“预览目标”和“读取档案并执行”。用户选择四种培养内容之一；仅指定武器模式不要求选择角色，也不读取角色档案。
- 脚本通过 BetterGI 0.64 的 `characterDevelopmentTask.GetCharacter()` 读取角色等级/上限、天赋界面等级与命座加成、武器名称及等级/上限。目标内部同时保存等级和等级上限，能够区分 `80/80` 与 `80/90`；命座 `+3` 会还原为实际培养等级。
- 自动目标复用现有材料计算器，不维护第二套成本算法。识别原始字段和最终目标保存到 `record/latest-profile.json`，计划、历史和邮件使用相同的“当前 → 目标”摘要。一星初始武器按规则库 `rarity=1` 忽略。
- 角色等级、天赋和武器独立判断。当前进度达到或超过目标时只跳过该项；单项字段缺失或识别失败时记录该项目标失败，其他可独立计算的目标继续。所有所选目标均无法计算时才在背包扫描前停止；文字识别异常提示检查 OCR V6，不猜测默认值。
- OCR V6 下已完成多轮角色定位、筛选和等级识别验证。当前版本已接入自动档案：按用户选择读取角色等级、天赋和当前佩戴武器，并把识别结果交给同一套材料计算与调度流程；武器也可改为手动指定或完全不培养。识别异常会在操作游戏前停止，不会阻断仍可独立成立的培养目标。
- 米游社养成计算器能够提供角色等级、天赋和佩戴武器数据，可作为开发阶段的对照数据及可选本地快照来源；第一版不把浏览器登录态作为正式运行的强制依赖。
- 装备为一星初始武器时自动忽略武器培养；规则按静态数据中的 `rarity=1` 判断，对应无锋剑、训练大剑、新手长枪、学徒笔记和猎弓。
- 识别失败、角色不存在、武器未知或数据不完整时停止生成受影响的目标并发送提醒，禁止按 1 级或默认武器盲目计算；独立的指定武器目标不被角色识别失败连带终止。

验收：四种培养内容均只生成对应目标；角色或某项天赋已经达到目标时不阻断武器，角色档案读取失败时独立的指定武器仍可继续。全部目标已达到时不读背包、不检查路线也不执行刷取。普通佩戴武器自动加入，初始一星武器自动忽略；仅预览不读背包或执行任务，识别后执行仍受顶部执行确认保护。

## 阶段 7：缺失路线自动匹配与原生订阅（预研）

目标：用户只选择培养目标后，脚本除自动匹配材料来源外，还能识别缺少的地方特产/怪物路线，并给出可核验、可一次确认的路线准备方案；不要求用户理解仓库目录，也不静默下载或执行远程内容。

### 已确认的 BetterGI 能力边界

- 当前 JS `pathingScript` 只提供路线读取、存在性检查和执行能力，没有向普通脚本公开仓库订阅或写入 `User/AutoPathing` 的接口。
- BetterGI 主程序内部已有脚本仓库订阅、更新记录和 `bettergi://script?import=...` 导入机制；原生导入会展示确认界面，但该能力目前不能由 JS 直接调用。
- JS 可通过网络请求自行下载 JSON，但这不属于 BetterGI 原生订阅，无法自然继承来源、更新和冲突管理，也会扩大远程内容与文件写入风险，因此不作为正式方案。

### 第一阶段：路线订阅助手（无需修改 BetterGI）

1. 从官方 `bettergi-scripts-list` 的路线目录生成版本化的 `route-catalog.json`，把角色材料映射到明确的官方路线相对路径、路线类型、作者/变体和队伍要求。
2. 计算实际材料缺口后扫描本机已安装路线，将结果分为“已安装可执行”“官方仓库可订阅”“暂时没有可靠路线”三类。
3. 默认只推荐经过核对的具体路线包；同一材料存在全收集、高效、草神或不同作者等多个变体时，不自动订阅整个材料目录，由用户选择或保持提醒。
4. 为缺失路线生成清晰的订阅清单和 BetterGI 原生导入数据/链接，保存到运行记录中；用户在 BetterGI 的原生确认界面完成订阅后，重新运行即可被现有路线发现器自动匹配。
5. 保留 `route-overrides.json` 人工路径覆盖，作为官方仓库缺少路线、目录变更或用户使用自建路线时的兜底。

### 第二阶段：申请 BetterGI 受控订阅接口

- 向 BetterGI 提议面向 JS 的交互式接口，例如 `repoSubscription.requestImport(paths)`；不在本项目中直接修改 BetterGI 主程序。
- 接口只接受当前官方仓库中的相对路径，限定在 `pathing/`，拒绝绝对路径、目录穿越和任意 URL。
- 必须复用 BetterGI 原生确认界面，向用户展示新增/覆盖文件；用户取消时不得写入。由 BetterGI 负责订阅记录、更新、去重和失败回滚，并返回已安装、已取消、缺失、失败等结构化状态。
- 若后续 BetterGI 正式发布该接口，脚本进行版本/能力检测：新版本可在用户确认后重新扫描并继续运行，旧版本继续使用第一阶段的订阅助手。

### 安全与发布边界

- 首期只索引官方脚本仓库路线，不自动接入第三方仓库；第三方来源需另行解决作者许可、来源标识、内容校验、更新与回滚后再评估。
- 不静默联网、不绕过 BetterGI 确认界面、不自动选择不确定的路线变体、不重复订阅已安装路线。
- 此功能不进入当前 0.9.0 和官方 PR #3496，作为后续版本预研；在接口、数据格式和用户确认流程稳定前不承诺“自动订阅”。

验收：用户选择角色培养目标后，脚本能准确列出缺失路线及其官方来源；完成一次原生确认后，路线可被重新发现和执行。整个流程没有静默下载、重复路线或整目录误订阅，无法可靠匹配时会明确提醒并保留人工覆盖入口。

## 阶段 8：可选扩展
- 来源级队伍和战斗策略覆盖。
- 更易用的可视化目标编辑与历史统计展示。

## 每阶段共同验收原则

- 先完成本地计算与配置校验；需要验证新执行器或新映射时再自愿使用单次模式，不把单次测试设为首次使用前置条件。
- 失败、未知和未确认收益必须显式呈现，不能默认为成功。
- 每次功能上线同步更新 `开发TODO.md`、`技术方案.md`、`项目计划.md` 与 README。

## 连续开发与回归约定

本项目采用“先持续开发、后集中实机回归”的节奏：不再因为等待游戏内测试而暂停实现。对无法在本地自动验证的 BetterGI 调用、OCR 结果、传送路径和战斗结果，开发阶段默认接口行为符合官方定义，并在 TODO 中保留风险和回归步骤。

集中实测时按功能逐项回归；测试结果与假设不符时，以实测为准修正实现、映射或策略，再重新执行本地测试。未经实测的功能不会在文档中标记为“已验证”。
